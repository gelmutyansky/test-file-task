const commonErrors = {
    default:  'ERROR',
    noObject: 'NO OBJECT',
    noFile:   'NO FILE',
};

const filesFolder = './public/files';

module.exports = {
    commonErrors: commonErrors,
    filesFolder:  filesFolder,
};